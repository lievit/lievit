{
  "name": "breadcrumb",
  "type": "registry:jte",
  "description": "An accessible breadcrumb navigation trail: <nav aria-label> landmark wrapping an ordered list, real <a href> for non-current items, aria-current='page' on the current (last) item, separator aria-hidden (WAI-ARIA breadcrumb pattern). Server-first PARTIAL (ADR-0012, Wave 4): the trail is the server-rendered route, so there is no client state and no island. Replaces the <lv-breadcrumb> Lit island. Plain HTML + Tailwind + --lv-* tokens, zero JS, CSP-clean.",
  "dependencies": [],
  "registryDependencies": ["tokens"],
  "files": [
    {
      "path": "jte/breadcrumb.jte",
      "type": "registry:jte",
      "target": "lievit/breadcrumb.jte",
      "root": "jte"
    }
  ],
  "tokens": ["color", "space", "radius", "type", "ring"],
  "docs": "Copies the breadcrumb partial to your JTE root at lievit/breadcrumb.jte, reached as @template.lievit.breadcrumb(items=...). Pass items as a List<Map<String,String>> root-to-current, each map keyed label (+ optional href); the last item is rendered as the current page (aria-current='page', not a link). Set separator to change the divider glyph (default '/'). The source is yours: edit freely."
}
